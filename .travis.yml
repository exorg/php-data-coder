language: php
php:
  - 5.5
  - 5.6
  - 7.0
install:
  - printf "\n" | pecl install yaml
  - echo "extension=yaml.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - composer install
script: 
  - phpunit tests/
  - ./vendor/bin/phpcs src/ tests/
